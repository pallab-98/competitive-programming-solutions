#include <bits/stdc++.h>
using namespace std;

int main() {
    int caseTest;
    cin >> T;

    while (T--) {
        long long n;
        cin >> n;

        cout << n * n + 2 * n + 2 << endl;
    }

    return 0;
}